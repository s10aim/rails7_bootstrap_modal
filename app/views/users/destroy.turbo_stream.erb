<%= turbo_stream.update "users" do %>
  <%= render @users %>
  <%# NOTE: actionを指定しないと, showアクションへのリンクになる %>
  <%= paginate @users, params: { action: "index" } %>
<% end %>
<%= turbo_stream.replace "flash" do %>
  <%= render "partials/flash" %>
<% end %>
